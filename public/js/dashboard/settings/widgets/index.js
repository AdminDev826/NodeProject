$(document).ready(() => {
    initComponent();
    initEventHandler();
});

/** init components */
initComponent = () => {
    console.log('initComponent');
}

/** init event handlers */
initEventHandler = () => {
    console.log('initEventHandler');
}